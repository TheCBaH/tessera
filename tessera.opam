# This file is generated by dune, edit dune-project instead
opam-version: "2.0"
synopsis: "A portable pure terminal core"
description:
  "Tessera decodes terminal output into an immutable logical screen."
license: "MIT"
depends: [
  "dune" {>= "3.10"}
  "err_trace"
  "ppx_expect" {with-test}
  "js_of_ocaml" {with-test}
  "melange" {with-test}
  "ocaml" {>= "4.14"}
  "odoc" {with-doc}
]
build: [
  ["dune" "subst"] {dev}
  [
    "dune"
    "build"
    "-p"
    name
    "-j"
    jobs
    "@install"
    "@runtest" {with-test}
    "@doc" {with-doc}
  ]
]
