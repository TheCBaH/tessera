{
    "name": "ocaml/opam toolchains",
    "id": "ocaml",
    "version": "0.0.4",
    "description": "Install ocaml/opam toolchains",
    "options": {
        "base-packages": {
            "type": "string",
            "default": "dune ocaml-lsp-server ocamlformat ocamlformat-rpc",
            "description": "space-separated packages installed by default; override this list for minimal or specialized toolchains"
        },
        "packages": {
            "type": "string",
            "default": "",
            "description": "additional space-separated packages to install; use 'name#version' to pin a specific version (e.g. 'utop ocamlformat#0.28.0'). Installation fails if a package is unavailable"
        },
        "optional-packages": {
            "type": "string",
            "default": "",
            "description": "space-separated packages to install only when opam can solve them for the selected switch and platform; same 'name#version' pin syntax as 'packages' (e.g. 'melange')"
        },
        "version": {
            "type": "string",
            "default": "4.14.3",
            "description": "OCaml version"
        },
        "options": {
            "type": "string",
            "default": "",
            "description": "OPAM switch options"
        },
        "system-packages": {
            "type": "string",
            "default": "",
            "description": "additional system packages"
        },
        "repositories": {
            "type": "string",
            "default": "",
            "description": "comma-separated extra opam repositories to add before installing packages: 'name url' pairs (e.g. 'rocq-released https://rocq-prover.org/opam/released')"
        },
        "pin-packages": {
            "type": "string",
            "default": "",
            "description": "comma-separated packages to pin: use 'name#version' to pin a specific version, 'name url' or 'name version' to pin via opam pin add (e.g. 'pkg1#1.0.0,pkg2 https://repo.git#branch')"
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils"
    ]
}
