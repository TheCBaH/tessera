; Native ppx_expect suites, split by component. Aliases below run one layer at a time; @runtest
; (dune build @runtest / dune runtest / make test) aggregates all of them plus the JSOO/Melange
; runtime-fixture targets. See test/README.md for the full alias list and dependency rules.

(library
 (name tessera_runtime_fixture)
 (modes byte native melange)
 (modules runtime_fixture)
 (libraries tessera))

(test
 (name runtime_fixture_test)
 (modules runtime_fixture_test)
 (libraries tessera_runtime_fixture))

(executable
 (name js_smoke)
 (modules js_smoke)
 (modes js)
 (libraries tessera_runtime_fixture))

(alias
 (name jsoo)
 (deps js_smoke.bc.js))

(melange.emit
 (target melange-output)
 (alias tessera-melange)
 (modules melange_smoke)
 (module_systems commonjs)
 (libraries tessera_runtime_fixture))
